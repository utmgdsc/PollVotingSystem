export type AggregatedStudent = {
    pollId: string,
    courseCode: string,
    sequence: number,
    utorid: string,
    timestamp: string,
    pollName: string,
    description: string,
    answer: number
}
